<?xml version="1.0" encoding="utf-8"?>
<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <PropertyGroup Condition=" '$(RunConfiguration)' == 'Default' ">
    <StartAction>Project</StartAction>
    <StartArguments>find-conflict --solutionfilename=/Users/shahz.macbook/projects/RND/ShellTester/ShellTesterSolution.sln --projects-only</StartArguments>
    <ExternalConsole>true</ExternalConsole>
  </PropertyGroup>
  <PropertyGroup Condition=" '$(RunConfiguration)' == 'list-projects' ">
    <StartAction>Project</StartAction>
    <StartArguments>list-projects -s ../../../../nautilus-cli-solution.sln -n -p</StartArguments>
    <ExternalConsole>true</ExternalConsole>
  </PropertyGroup>
  <PropertyGroup Condition=" '$(RunConfiguration)' == 'list-projects with package updates' ">
    <StartAction>Project</StartAction>
    <StartArguments>list-projects -s ../../../../nautilus-cli-solution.sln -n -p -u</StartArguments>
    <ExternalConsole>true</ExternalConsole>
  </PropertyGroup>
  <PropertyGroup Condition=" '$(RunConfiguration)' == 'list-projects big-solution with package updates' ">
    <StartAction>Project</StartAction>
    <StartArguments>list-projects -p -n -u -s /Users/shahz.macbook/projects/synthphonic/nautilus/src/nautilus.sln</StartArguments>
    <ExternalConsole>true</ExternalConsole>
  </PropertyGroup>
</Project>